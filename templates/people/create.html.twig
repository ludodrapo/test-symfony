{% extends 'base.html.twig' %}

{% block title %}Nouvelle personne{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Créons ensemble une nouvelle personne</h1>

    {{ form_start(formView, {'action': path('people_create')}) }}

		<div class="d-flex justify-content-between align-items-center">
			<button type="submit" class="btn btn-success">Ajouter</button>
			{{ form_end(formView) }}
			{# <a href="{{ path('task_list') }}" class="btn btn-primary">Retour à la liste des tâches</a> #}
		</div>
    
</div>
{% endblock %}
